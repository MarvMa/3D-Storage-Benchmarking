FROM python:3.9-slim

WORKDIR /app

COPY requirements.txt .

RUN pip install --no-cache-dir -r requirements.txt

COPY benchmarks/locustfile.py .
COPY benchmarks/benchmark_files/model.gltf .

EXPOSE 8089

CMD ["locust", "-f", "locustfile.py"]
